#ifndef BUILDER_ENUMS_H
#define BUILDER_ENUMS_H

enum class DriverLicence {B, D};
enum class BusPassengerCategory {Adult = 0, Child = 1, Discount = 2};
enum class TaxiPassengerCategory {Adult = 0, Child = 1};

#endif //BUILDER_ENUMS_H
